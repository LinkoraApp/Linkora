[package]
name = "web_capture"
version = "0.1.0"
edition = "2024"

[lib]
name = "web_capture"
crate-type = ["cdylib"]
path = "src/main/rust/web_capture.rs"

[dependencies]
monolith = "2.10.1"
jni = "0.21"

[dev-dependencies]
tempfile = "3.27.0"
jni = { version = "0.21", features = ["invocation"] }